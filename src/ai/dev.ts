// Flows will be imported for their side effects in this file.
// Example: import './flows/diagnose-plant-flow';

// If you have specific Genkit flows, import them here so they are registered
// when running `genkit:dev` or `genkit:watch`.
// e.g. import './flows/your-flow-file';
